import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.findajob.app',
  appName: 'findajob',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
