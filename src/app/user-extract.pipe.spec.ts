import { UserExtractPipe } from './user-extract.pipe';

describe('UserExtractPipe', () => {
  it('create an instance', () => {
    const pipe = new UserExtractPipe();
    expect(pipe).toBeTruthy();
  });
});
